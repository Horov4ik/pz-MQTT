# Mosquitto minimal config with websockets enabled
listener 1883
protocol mqtt

listener 9001
protocol websockets

# persistence
persistence true
persistence_location /mosquitto/data/

log_type all
log_dest file /mosquitto/log/mosquitto.log

# allow anonymous connections for lab (change for production)
allow_anonymous true
